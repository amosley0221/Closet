{
  "name": "closet",
  "version": "1.0.0",
  "private": true,
  "description": "Closet — your wardrobe PWA (Studio direction)",
  "scripts": {
    "build": "node build.mjs",
    "start": "node build.mjs && npx --yes serve dist -l ${PORT:-8000}",
    "serve": "npx --yes serve dist -l ${PORT:-8000}"
  },
  "dependencies": {
    "esbuild": "0.25.0",
    "react": "18.3.1",
    "react-dom": "18.3.1"
  }
}
